# A first post

This post exercises every Markdown construct the `content` command maps onto
the Entangled block grammar.

It has **bold**, *italic*, `inline code`, and ~~strikethrough~~ text, plus a
[citation link](https://example.org/source) and a
[same-site link](/articles/another).

## A list

- first item
- second item with **bold**
- third item

## Some code

```rust
fn main() {
    println!("hello, entangled");
}
```

> A short quotation on its own.

An image on its own line, whose hash and dimensions the tool reads from the
file:

![A small sample image](/assets/photo.png)

---

That divider above ends the post.
